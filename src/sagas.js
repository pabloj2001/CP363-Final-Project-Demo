import { call, put , takeEvery, takeLastest } from 'redux-saga/effects';

function *accessCart(action){
    try { 

    } catch (err) { 

    }

}

function *mySaga() {
    yield ("GO_TO_CART", accessCart);
};

export default mySaga;   